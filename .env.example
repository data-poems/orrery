# Optional NASA NeoWs proxy (e.g. Cloudflare Worker at https://orrery.solar/api/neo?date={date})
# VITE_NEO_FEED_URL=https://orrery.solar/api/neo?start_date={date}&end_date={date}

# Direct NASA API key when not using a proxy (defaults to DEMO_KEY)
# VITE_NASA_API_KEY=
